import { EqualHeightColDirective } from './equal-height-col.directive';

describe('EqualHeightColDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualHeightColDirective();
    expect(directive).toBeTruthy();
  });
});
