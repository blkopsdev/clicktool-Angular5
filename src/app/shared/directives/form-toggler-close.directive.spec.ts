import { FormTogglerCloseDirective } from './form-toggler-close.directive';

describe('FormTogglerCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new FormTogglerCloseDirective();
    expect(directive).toBeTruthy();
  });
});
