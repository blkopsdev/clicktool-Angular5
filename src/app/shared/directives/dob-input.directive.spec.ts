import { DobInputDirective } from './dob-input.directive';

describe('DobInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DobInputDirective();
    expect(directive).toBeTruthy();
  });
});
