import { InputAnimationDirective } from './input-animation.directive';

describe('InputAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new InputAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
